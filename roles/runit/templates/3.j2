#!/usr/bin/ash
exec 2>&1

PATH=/usr/local/bin:/usr/bin

LAST=0
test -x /etc/runit/reboot && LAST=6

echo 'Waiting for services to stop...'
sv -w196 force-stop {{ runit_service_dir }}/*
sv exit {{ runit_service_dir }}/*

echo 'Shutdown...'
/etc/init.d/rc $LAST
